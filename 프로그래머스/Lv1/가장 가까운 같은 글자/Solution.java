class Solution {
    public int[] solution(String s) {
        int[] answer = new int[s.length()];
        
        // for(int i = 0; i < s.length(); i++) {
        //     char word = s.charAt(i);
        //     answer[i] = s.lastIndexOf(word);
        // }
        
        return answer;
    }
}