class Solution {
    public int solution(int n, int w, int num) {
        int answer = 0;
        int max = n / w + 1;
        

        indexOf(num) % w
            
        return answer;
    }
}